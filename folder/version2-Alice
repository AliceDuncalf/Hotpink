##Funktionaliteter 
--filtrera ut program genom att klicka på kategorier
--Få ut 4 divar genom filtrering av land, stad, universitet och program med titel, info och herobild på land och stad, språk, visa,   

##Sidor
--index.html :: Header, nav, main och footer (allt innehåll)
--globalVariables.js :: De variablar som vi kan återanvända och applicera i vår mainscript, även navfunktionen
--main.js :: innehåller alla funktioner till våra två huvudfunktioner
--database.js :: databasen med objects och arrays 


##Funktioner
--createCategoriesContent () :: klicka på en kategori och så skapas innehåll med program, land och stad
--createCountryContent (country) :: (countryContent) skapa det innehåll som finns i country - flagga, bild, info, visa, språk
--createCityContent (city) :: (cityContent) skapa det innehåll som finns i city - soldagar, bild, info, språk
--createUniversityContent (university) :: (universityContent) skapa det innehåll som finns i university -
--createProgramContent (program) :: (programContent) skapa det innehåll som finns i program - språk, nivå 

--getProgramByCategory (category) :: program[], filtrera ut och visa program genom att klicka på kategorier 
--getCountryByCategory (category) :: country[], filtrera ut och visa land genom att klicka på kategorier 
--getCityByCategory (category) :: city[], filtrera ut och visa stad genom att klicka på kategorier 

*/--filterCitiesByCountry (country) :: cities[], filtrera ut städer beroende på vilket land som är valt
--filterUniversitiesByCity (city) :: universities[], filtrera ut universitet kopplat till staden som är vald
--filterProgramsByUniversity (university) :: programs [], filtrerar ut program som finns på universitetet */


##Globala variabler
--let DB =
--let categories = [categories]
--let selectCountry = [countries]
--let selectCity = [cities]
--let selectUniversity = [universities]
--let selectProgram = [programs]

/*--let divProgramAndCategories =
--let divFilterContainer = 
--let categories = {}
--let programGeneralContent = {}
--let countryGeneralContent = {}
--let cityGeneralContent = {}
--let universityGeneralContent= {}
--let allContent = {programGeneralContent, countryGeneralContent, cityGeneralContent, universityGeneralContent} */

##Css-filer
--variables.css :: Stylar variablar som inte förändras; fontstil, h1:or, fontstorlek, bakgrundsfärg, bild, storlek, vw, nav som hoppar till rätt del på html-sidan, header, footer, heropicture, infobox
--function.css :: Stylar funktionerna och innehållet 
